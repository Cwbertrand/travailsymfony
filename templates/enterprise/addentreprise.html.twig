{% extends 'base.html.twig' %}

{% block title %}Hello Enterprise Controller!{% endblock %}

{% block body %}
    <div class="container py-5">
        {% if edit %}
            <h1>Edit l'Entreprise</h1>
        {% else %}
            <h1>Ajouter une Entreprise</h1>
        {% endif %}

        {{form(form)}}
    </div>
{% endblock %}
